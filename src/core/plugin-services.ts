import { Service } from "@elizaos/core";

/**
 * Plugin Services Module
 * 
 * This module organizes all services and provides functions
 * to retrieve them for plugin registration.
 */

/**
 * Get all services for plugin registration
 * 
 * @returns Array of all services available in the plugin
 */
export function getAllServices(): (typeof Service)[] {
  // TODO: Import and return actual services once module structure is established
  return [];
} 