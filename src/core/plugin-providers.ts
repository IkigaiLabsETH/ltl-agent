import type { Provider } from "@elizaos/core";

/**
 * Plugin Providers Module
 * 
 * This module organizes all providers and provides functions
 * to retrieve them for plugin registration.
 */

/**
 * Get all providers for plugin registration
 * 
 * @returns Array of all providers available in the plugin
 */
export function getAllProviders(): Provider[] {
  // TODO: Import and return actual providers once module structure is established
  return [];
} 